# {{PROJECT_NAME}}

- 프로젝트 ID: `{{PROJECT_ID}}`
- 고객명: {{CUSTOMER_NAME}}
- 생성일: {{CREATED_AT}}

## Workflow

1. `scope.md`에 허용 범위와 제외 범위를 기록합니다.
2. `target add`로 웹사이트를 등록합니다.
3. `finding add`로 웹별 취약점을 등록합니다.
4. 보고서용 증적은 반드시 마스킹 후 `evidence/report`에 저장합니다.
5. `validate` 후 `report build` 또는 `export ppt`를 실행합니다.
